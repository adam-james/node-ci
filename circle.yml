machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t adamjames/node-ci:init .

test:
  override:
    - docker run adamjames/node-ci:init npm run test
